/* seria una clase que deberia de estar inicializada o con un constructor */
export interface Productos{
    id: number;
    nombre: string;
    cantidad: number;
    precio: number;
    img: string;
    total: number;  
}